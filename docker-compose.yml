version: '3.8'

services:
  web-frontend:
    build:
      context: .
    container_name: ai-work-flow-front
    restart: always
    ports:
      - "8080:80"
    networks:
      - frontend-net

networks:
  frontend-net:
    driver: bridge